# Cloudflare Worker / Pages. `name` must match the dashboard project name.
name = "thenewsoulsearchersblog"
compatibility_date = "2026-08-23"
pages_build_output_dir = "dist"

[assets]
directory = "./dist"
