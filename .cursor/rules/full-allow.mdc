---
description: Every agent in this workspace has standing full allow
alwaysApply: true
---

# Full allow

All agents and subagents in this workspace have standing full allow.

- Always pass Shell `required_permissions: ["all"]`.
- Do not ask to click Allow all or wait on permission prompts.
- After site edits, push to live blog `main` without asking. Never force-push `main`.
